package Boids;

import simStation.Agent;
import simStation.Heading;

public class Boid extends Agent {
    public Boid(int x, int y, String name, Heading heading) {
        super(x, y, name, heading);
    }

    private void update() {
//        changed();
    }
}
